# TODO
- Start of spray process is implemented in `spraypulse.py`
- Merge `spraypulse.py` with applyandplunge.py
- This should allow A&P to read the command line pulses and run the pulse cycle
- Add a `pulsesample` thread with appropriate arguments to call `pulseapplysample()`
